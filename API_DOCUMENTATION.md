# 医药宝管理系统 - API接口文档

> **版本**: v1.0 | **更新日期**: 2026-04-08

---

## 目录

1. [认证相关接口](#一认证相关接口)
2. [用户管理接口](#二用户管理接口)
3. [药品管理接口](#三药品管理接口)
4. [分类管理接口](#四分类管理接口)
5. [厂商管理接口](#五厂商管理接口)
6. [药箱管理接口](#六药箱管理接口)
7. [图片上传接口](#七图片上传接口)

---

## 一、认证相关接口

### 1. 用户登录

| 属性 | 内容 |
|------|------|
| **接口地址** | `/api/token/` |
| **请求方式** | POST |
| **接口说明** | 用户登录获取 JWT Token |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | string | 是 | 用户名 |
| password | string | 是 | 加密后的密码 |

**响应示例：**
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "user": {
    "id": 1,
    "username": "admin",
    "email": "admin@example.com",
    "name": "管理员",
    "mobile": "13800138000",
    "is_admin": true,
    "is_staff": true
  }
}
```

---

### 2. 刷新 Token

| 属性 | 内容 |
|------|------|
| **接口地址** | `/api/refresh/` |
| **请求方式** | POST |
| **接口说明** | 使用 Refresh Token 刷新 Access Token |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| refresh | string | 是 | Refresh Token |

**响应示例：**
```json
{
  "access": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9..."
}
```

---

### 3. 用户注册

| 属性 | 内容 |
|------|------|
| **接口地址** | `/api/register/` |
| **请求方式** | POST |
| **接口说明** | 新用户注册（默认注册为普通用户） |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | string | 是 | 用户名（3-20字符） |
| password | string | 是 | 加密后的密码（至少6位） |
| password_confirm | string | 是 | 确认密码（加密后） |
| mobile | string | 否 | 手机号 |
| name | string | 否 | 真实姓名 |

**响应示例：**
```json
{
  "message": "注册成功",
  "access": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "refresh": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "user": {
    "id": 2,
    "username": "zhangsan",
    "name": "张三",
    "mobile": "13800138001",
    "is_admin": false
  }
}
```

---

### 4. 获取当前用户信息

| 属性 | 内容 |
|------|------|
| **接口地址** | `/api/me/` |
| **请求方式** | GET |
| **接口说明** | 获取当前登录用户的详细信息 |
| **权限要求** | 需登录 |

**响应示例：**
```json
{
  "id": 1,
  "username": "admin",
  "name": "管理员",
  "mobile": "13800138000",
  "email": "admin@example.com",
  "sex": 1,
  "birthday": "1990-01-01",
  "is_admin": true,
  "is_active": true,
  "is_staff": true,
  "date_joined": "2026-01-01T08:00:00",
  "last_login": "2026-04-08T10:30:00"
}
```

---

### 5. 修改当前用户信息

| 属性 | 内容 |
|------|------|
| **接口地址** | `/api/me/` |
| **请求方式** | PUT/PATCH |
| **接口说明** | 修改当前登录用户的信息 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 否 | 真实姓名 |
| mobile | string | 否 | 手机号 |
| email | string | 否 | 邮箱 |
| sex | int | 否 | 性别（1男/2女/3保密） |
| birthday | date | 否 | 生日 |

---

### 6. 修改密码

| 属性 | 内容 |
|------|------|
| **接口地址** | `/api/change_password/` |
| **请求方式** | POST |
| **接口说明** | 修改当前用户密码 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| old_password | string | 是 | 原密码 |
| new_password | string | 是 | 新密码（至少6位） |
| confirm_password | string | 是 | 确认新密码 |

**响应示例：**
```json
{
  "message": "密码修改成功"
}
```

---

## 二、用户管理接口

> 仅管理员可访问

### 1. 获取用户列表

| 属性 | 内容 |
|------|------|
| **接口地址** | `/users/` |
| **请求方式** | GET |
| **接口说明** | 获取所有用户列表（支持搜索和筛选） |
| **权限要求** | 管理员 |

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | int | 否 | 页码（默认1） |
| page_size | int | 否 | 每页数量（默认10） |
| search | string | 否 | 搜索关键词（用户名/姓名/手机号） |
| role | string | 否 | 角色筛选（admin/user） |
| is_active | string | 否 | 状态筛选（true/false） |

**响应示例：**
```json
{
  "count": 100,
  "next": "http://localhost:8000/users/?page=2",
  "previous": null,
  "results": [
    {
      "id": 1,
      "username": "admin",
      "name": "管理员",
      "mobile": "13800138000",
      "email": "admin@example.com",
      "sex": 1,
      "sex_display": "男",
      "is_admin": true,
      "is_active": true,
      "date_joined": "2026-01-01T08:00:00",
      "last_login": "2026-04-08T10:30:00"
    }
  ]
}
```

---

### 2. 创建用户

| 属性 | 内容 |
|------|------|
| **接口地址** | `/users/` |
| **请求方式** | POST |
| **接口说明** | 创建新用户 |
| **权限要求** | 管理员 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | string | 是 | 用户名（3-20字符） |
| password | string | 是 | 密码（至少6位） |
| name | string | 否 | 真实姓名 |
| mobile | string | 否 | 手机号 |
| email | string | 否 | 邮箱 |
| sex | int | 否 | 性别（1男/2女/3保密，默认1） |
| is_admin | boolean | 否 | 是否管理员（默认false） |
| is_active | boolean | 否 | 是否启用（默认true） |

---

### 3. 获取用户详情

| 属性 | 内容 |
|------|------|
| **接口地址** | `/users/{id}/` |
| **请求方式** | GET |
| **接口说明** | 获取指定用户的详细信息 |
| **权限要求** | 管理员 |

---

### 4. 更新用户

| 属性 | 内容 |
|------|------|
| **接口地址** | `/users/{id}/` |
| **请求方式** | PUT/PATCH |
| **接口说明** | 更新指定用户的信息 |
| **权限要求** | 管理员 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| password | string | 否 | 新密码（不提供则不修改） |
| name | string | 否 | 真实姓名 |
| mobile | string | 否 | 手机号 |
| email | string | 否 | 邮箱 |
| sex | int | 否 | 性别 |
| birthday | date | 否 | 生日 |
| is_admin | boolean | 否 | 是否管理员 |
| is_active | boolean | 否 | 是否启用 |

---

### 5. 删除用户

| 属性 | 内容 |
|------|------|
| **接口地址** | `/users/{id}/` |
| **请求方式** | DELETE |
| **接口说明** | 删除指定用户 |
| **权限要求** | 管理员 |

---

### 6. 批量删除用户

| 属性 | 内容 |
|------|------|
| **接口地址** | `/users/batch_delete/` |
| **请求方式** | DELETE |
| **接口说明** | 批量删除多个用户 |
| **权限要求** | 管理员 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| user_ids | array | 是 | 要删除的用户ID数组 |

**请求示例：**
```json
{
  "user_ids": [2, 3, 4]
}
```

**响应示例：**
```json
{
  "message": "成功删除 3 个用户",
  "deleted_count": 3
}
```

---

### 7. 重置用户密码

| 属性 | 内容 |
|------|------|
| **接口地址** | `/users/{id}/reset_password/` |
| **请求方式** | POST |
| **接口说明** | 重置指定用户的密码 |
| **权限要求** | 管理员 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| password | string | 否 | 新密码（默认123456） |

**响应示例：**
```json
{
  "message": "用户 zhangsan 的密码已重置",
  "user_id": 2
}
```

---

### 8. 切换用户状态

| 属性 | 内容 |
|------|------|
| **接口地址** | `/users/{id}/toggle_status/` |
| **请求方式** | POST |
| **接口说明** | 启用/禁用用户（切换状态） |
| **权限要求** | 管理员 |

**响应示例：**
```json
{
  "message": "用户 zhangsan 已禁用",
  "is_active": false
}
```

---

### 9. 获取用户统计

| 属性 | 内容 |
|------|------|
| **接口地址** | `/users/statistics/` |
| **请求方式** | GET |
| **接口说明** | 获取用户统计数据 |
| **权限要求** | 管理员 |

**响应示例：**
```json
{
  "total": 100,
  "admin_count": 5,
  "user_count": 95,
  "active_count": 90,
  "inactive_count": 10,
  "today_count": 3
}
```

---

## 三、药品管理接口

### 1. 获取药品列表

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/drug/` |
| **请求方式** | GET |
| **接口说明** | 获取药品列表（分页） |

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | int | 否 | 页码（默认1） |
| page_size | int | 否 | 每页数量（默认50） |

---

### 2. 新增药品

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/drug/` |
| **请求方式** | POST |
| **接口说明** | 创建新药品 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| drug_name | string | 是 | 药品名 |
| trade_name | string | 否 | 商品名 |
| specification | string | 否 | 规格 |
| dosage_form | string | 否 | 剂型 |
| manufacturer | int | 否 | 生产厂商ID |
| manufacturer_holder | int | 否 | 上市许可持有人ID |
| type2_drug | int | 否 | 二级分类ID |
| approval_number | string | 否 | 批准文号 |
| atc_code | string | 否 | ATC代码 |
| medical_insurance | string | 否 | 医保类型 |
| is_hot | boolean | 否 | 是否热门 |
| description | text | 否 | 药品说明 |
| indications | text | 否 | 适用症状 |

---

### 3. 获取药品详情

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/drug/{id}/` |
| **请求方式** | GET |
| **接口说明** | 获取药品详细信息 |

---

### 4. 修改药品

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/drug/{id}/` |
| **请求方式** | PUT/PATCH |
| **接口说明** | 更新药品信息 |
| **权限要求** | 需登录 |

---

### 5. 删除药品

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/drug/{id}/` |
| **请求方式** | DELETE |
| **接口说明** | 删除药品 |
| **权限要求** | 需登录 |

---

### 6. 批量删除药品

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/batch_delete_drugs/` |
| **请求方式** | DELETE |
| **接口说明** | 批量删除多个药品 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| drug_ids | array | 是 | 要删除的药品ID数组 |

**请求示例：**
```json
{
  "drug_ids": [1, 2, 3]
}
```

---

### 7. 搜索药品

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/search_anything/` |
| **请求方式** | POST |
| **接口说明** | 全局模糊搜索药品 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| text | string | 是 | 搜索关键词 |

**搜索范围：**
- 药品名（drug_name）
- 药品英文名（drug_name_en）
- 商品名（trade_name）
- 商品名英文名（trade_name_en）
- 生产厂商名称
- 厂商简称

---

### 8. Excel批量导入药品

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/add_drugs_from_excel/` |
| **请求方式** | POST |
| **接口说明** | 通过Excel文件批量导入药品 |
| **Content-Type** | multipart/form-data |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | file | 是 | Excel文件（.xls/.xlsx） |

---

### 9. 下载导入模板

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/download_import_template/` |
| **请求方式** | GET |
| **接口说明** | 下载药品导入模板Excel文件 |

---

### 10. 预览导入数据

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/preview_import_excel/` |
| **请求方式** | POST |
| **接口说明** | 预览Excel导入数据（不保存到数据库） |
| **Content-Type** | multipart/form-data |

---

### 11. 异步批量导入

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/async_import_drugs/` |
| **请求方式** | POST |
| **接口说明** | 异步导入（后台线程处理，适合大批量数据） |
| **Content-Type** | multipart/form-data |

**响应示例：**
```json
{
  "task_id": "import_abc123",
  "status": "processing",
  "message": "导入任务已提交"
}
```

---

### 12. 查询导入任务状态

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/import_task_status/{task_id}/` |
| **请求方式** | GET |
| **接口说明** | 查询异步导入任务进度 |

**响应示例：**
```json
{
  "task_id": "import_abc123",
  "status": "completed",
  "progress": 100,
  "message": "导入完成",
  "result": {
    "created_count": 100,
    "skipped_count": 5,
    "error_count": 0
  }
}
```

---

## 四、分类管理接口

### 1. 获取一级分类列表

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/type1drug/` |
| **请求方式** | GET |
| **接口说明** | 获取所有一级分类 |

---

### 2. 创建一级分类

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/type1drug/` |
| **请求方式** | POST |
| **接口说明** | 创建新的一级分类 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 分类名称 |

---

### 3. 更新一级分类

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/type1drug/{id}/` |
| **请求方式** | PUT/PATCH |
| **接口说明** | 更新一级分类 |
| **权限要求** | 需登录 |

---

### 4. 删除一级分类

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/type1drug/{id}/` |
| **请求方式** | DELETE |
| **接口说明** | 删除一级分类（有子分类时无法删除） |
| **权限要求** | 需登录 |

---

### 5. 获取二级分类列表

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/type2drug/` |
| **请求方式** | GET |
| **接口说明** | 获取所有二级分类 |

---

### 6. 创建二级分类

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/type2drug/` |
| **请求方式** | POST |
| **接口说明** | 创建新的二级分类 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 分类名称 |
| type1_drug | int | 是 | 所属一级分类ID |

---

### 7. 获取分类树结构

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/all_type1_with_type2/` |
| **请求方式** | GET |
| **接口说明** | 获取所有一级分类及其子分类（树形结构） |

**响应示例：**
```json
{
  "count": 10,
  "results": [
    {
      "value": 1,
      "label": "化学药品",
      "children": [
        {"value": 1, "label": "抗生素"},
        {"value": 2, "label": "解热镇痛"}
      ]
    }
  ]
}
```

---

### 8. 获取一级分类下的二级分类

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/type1_type2/{type1_id}/` |
| **请求方式** | GET |
| **接口说明** | 获取指定一级分类下的所有二级分类 |

---

## 五、厂商管理接口

### 1. 获取生产厂商列表

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/manufacturer/` |
| **请求方式** | GET |
| **接口说明** | 获取所有生产厂商 |

---

### 2. 创建生产厂商

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/manufacturer/` |
| **请求方式** | POST |
| **接口说明** | 创建新的生产厂商 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 厂商全称 |
| abbreviation | string | 否 | 厂商简称 |

---

### 3. 更新生产厂商

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/manufacturer/{id}/` |
| **请求方式** | PUT/PATCH |
| **接口说明** | 更新生产厂商信息 |
| **权限要求** | 需登录 |

---

### 4. 删除生产厂商

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/manufacturer/{id}/` |
| **请求方式** | DELETE |
| **接口说明** | 删除生产厂商（有关联药品时无法删除） |
| **权限要求** | 需登录 |

---

### 5. 获取上市许可持有人列表

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/manufacturerholder/` |
| **请求方式** | GET |
| **接口说明** | 获取所有上市许可持有人 |

---

### 6. 创建上市许可持有人

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/manufacturerholder/` |
| **请求方式** | POST |
| **接口说明** | 创建新的上市许可持有人 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 持有人全称 |
| abbreviation | string | 否 | 持有人简称 |

---

### 7. 更新上市许可持有人

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/manufacturerholder/{id}/` |
| **请求方式** | PUT/PATCH |
| **接口说明** | 更新上市许可持有人信息 |
| **权限要求** | 需登录 |

---

### 8. 删除上市许可持有人

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/manufacturerholder/{id}/` |
| **请求方式** | DELETE |
| **接口说明** | 删除上市许可持有人 |
| **权限要求** | 需登录 |

---

## 六、药箱管理接口

### 1. 获取药箱列表

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/cabinets/` |
| **请求方式** | GET |
| **接口说明** | 获取当前用户的所有药箱 |
| **权限要求** | 需登录 |

---

### 2. 创建药箱

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/cabinets/` |
| **请求方式** | POST |
| **接口说明** | 创建新药箱 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 药箱名称 |
| cabinet_type | string | 否 | 药箱类型（home/travel/office/other，默认home） |
| description | string | 否 | 药箱描述 |
| is_default | boolean | 否 | 是否设为默认药箱 |

---

### 3. 更新药箱

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/cabinets/{id}/` |
| **请求方式** | PUT/PATCH |
| **接口说明** | 更新药箱信息 |
| **权限要求** | 需登录（只能修改自己的药箱） |

---

### 4. 删除药箱

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/cabinets/{id}/` |
| **请求方式** | DELETE |
| **接口说明** | 删除药箱 |
| **权限要求** | 需登录（不能删除默认药箱） |

---

### 5. 获取默认药箱

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/default_cabinet/` |
| **请求方式** | GET |
| **接口说明** | 获取或创建默认药箱 |
| **权限要求** | 需登录 |

**响应示例：**
```json
{
  "cabinet": {
    "id": 1,
    "name": "默认药箱",
    "cabinet_type": "home",
    "is_default": true
  },
  "created": false
}
```

---

### 6. 获取药箱中的药品

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/cabinets/{cabinet_id}/drugs/` |
| **请求方式** | GET |
| **接口说明** | 获取指定药箱中的所有药品 |
| **权限要求** | 需登录 |

**查询参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| filter | string | 否 | 筛选类型（all/valid/expiring_soon/expired/low_stock） |
| sort | string | 否 | 排序字段（默认-added_at） |

---

### 7. 添加药品到药箱

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/cabinet_drugs/` |
| **请求方式** | POST |
| **接口说明** | 添加药品到指定药箱 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| cabinet | int | 是 | 药箱ID |
| drug | int | 是 | 药品ID |
| quantity | int | 否 | 数量（默认1） |
| unit | string | 否 | 单位（片/粒/瓶等） |
| production_date | date | 否 | 生产日期 |
| valid_until | date | 否 | 有效期至 |
| batch_number | string | 否 | 批号 |
| remind_before_days | int | 否 | 过期前提醒天数（默认7） |
| notes | string | 否 | 备注 |

---

### 8. 更新药箱药品

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/cabinet_drugs/{id}/` |
| **请求方式** | PUT/PATCH |
| **接口说明** | 更新药箱中药品的信息 |
| **权限要求** | 需登录 |

---

### 9. 删除药箱药品

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/cabinet_drugs/{id}/` |
| **请求方式** | DELETE |
| **接口说明** | 从药箱中移除药品 |
| **权限要求** | 需登录 |

---

### 10. 更新药品数量

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/cabinet_drugs/{id}/quantity/` |
| **请求方式** | POST |
| **接口说明** | 增加或减少药品数量 |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| action | string | 是 | 操作类型（increase/decrease） |
| amount | int | 否 | 数量（默认1） |

---

### 11. 获取过期药品统计

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/expiring_drugs/` |
| **请求方式** | GET |
| **接口说明** | 获取即将过期和已过期的药品统计 |
| **权限要求** | 需登录 |

**响应示例：**
```json
{
  "expired_count": 2,
  "expiring_soon_count": 5,
  "valid_count": 20,
  "total_count": 27,
  "expired_drugs": [...],
  "expiring_soon_drugs": [...]
}
```

---

## 七、图片上传接口

### 1. 上传药品图片

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/drugs/{drug_id}/upload_image/` |
| **请求方式** | POST |
| **接口说明** | 为指定药品上传图片 |
| **Content-Type** | multipart/form-data |
| **权限要求** | 需登录 |

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| image | file | 是 | 图片文件（jpg/png/gif/webp，最大10MB） |

**响应示例：**
```json
{
  "success": true,
  "message": "图片上传成功",
  "drug_id": 1,
  "drug_name": "阿莫西林胶囊",
  "image_url": "http://localhost:8000/media/drug_images/1_阿莫西林胶囊.jpg",
  "filename": "1_阿莫西林胶囊.jpg"
}
```

---

### 2. 删除药品图片

| 属性 | 内容 |
|------|------|
| **接口地址** | `/syyb/drugs/{drug_id}/delete_image/` |
| **请求方式** | DELETE |
| **接口说明** | 删除药品的图片 |
| **权限要求** | 需登录 |

**响应示例：**
```json
{
  "success": true,
  "message": "图片删除成功",
  "drug_id": 1,
  "drug_name": "阿莫西林胶囊"
}
```

---

## 附录

### 通用响应格式

#### 成功响应
```json
{
  "count": 100,
  "results": [...],
  "next": "http://api/next/page",
  "previous": null
}
```

#### 错误响应
```json
{
  "error": "错误信息描述"
}
```

### HTTP 状态码

| 状态码 | 说明 |
|--------|------|
| 200 | 请求成功 |
| 201 | 创建成功 |
| 204 | 删除成功（无返回内容） |
| 400 | 请求参数错误 |
| 401 | 未授权（Token无效或过期） |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 请求头要求

所有需要登录的接口需要在请求头中携带 Token：
```
Authorization: Bearer {access_token}
```

---

*文档结束*
